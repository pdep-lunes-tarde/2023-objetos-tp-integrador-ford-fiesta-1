import wollok.game.*

import bombas.*
import bomberman.*
import cajas.*
import juego.*
import powerups.*
import rivales.*

describe "pruebas de Bomberman"
{
	test "El primer bomberman inicia en la posicion (1 ; 1)" 
	{
		assert.equals(game.at(1,1),jugador1.position())
	}
	test "Mientras que el segundo bomberman inicia en la posicion (13 ; 13)" 
	{
		assert.equals(game.at(13,13),jugador2.position())
	}
}
